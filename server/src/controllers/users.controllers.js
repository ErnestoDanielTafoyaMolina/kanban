export const handleLogin = async(req, res)=>{
    res.json("Aquí ira la logica par hacer login");
}

export const handeLogOut = async(req, res)=>{
    res.json("Aqui irá la logica para hacer logout")
}